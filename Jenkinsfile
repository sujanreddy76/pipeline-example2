//tools
pipeline {
    agent {
        label 'java-label'
    }
    stages{
        stage('Maven version') {
            steps {
                echo 'Welcome to tools demo'
                sh 'mvn --version'
            }
        }
    }
}
